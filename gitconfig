[user]
	name = Benjamin Schmidt
	email = benschmidt@live.de
	username = schmidt
[core]
	editor = nvim
	whitespace = fix,-indent-with-non-tab,trailing-space,cr-at-eol
	excludesfile = ~/.dotfiles/gitignore
[web]
	browser = firefox
[init]
	templatedir = ~/.git_template
[push]
	default = matching
[color]
	ui = true
[color "branch"]
	current = yellow bold
	local = green bold
	remote = cyan bold
[color "diff"]
	meta = yellow bold
	frag = magenta bold
	old = red bold
	new = green bold
	whitespace = red reverse
[color "status"]
	added = green bold
	changed = yellow bold
	untracked = red bold
[diff]
	tool = nvim
[difftool "nvim"]
    cmd = ~/.programs/nvim.appimage 
	prompt = false
[gitlab]
    user = schmidt
    token = HjFkxTX23fhbMU-UB7yx
[github]
    user = benatouba
[overleaf]
    user = benjaminschmmidt88@gmail.com
[alias]
	ctags = !.git/hooks/ctags
# define command which will be used when "vim" is set as a merge tool
[mergetool "nvim"]
  cmd = ~/.programs/nvim.appimage -f -c \"Gvdiffsplit!\" \"$MERGED\"
# set "vim" as tool for merging
[merge]
  tool = nvim
# automatically launch merge tool without displaying a prompt
[mergetool]
  prompt = false
